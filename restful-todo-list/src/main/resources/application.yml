server:
  port: 8088

#spring:
#  datasource:
#    driver-class-name: com.mysql.cj.jdbc.Driver
#    url: jdbc:mysql://127.0.0.1:3306/my_db?characterEncoding=UTF-8&serverTimezone=UTC
#    username: root
#    password: mymymysql
#    data: data.sql

# data.sql이 안먹힌다...!! 왜지?

spring:
  jpa:
    show-sql: true
  messages:
    basename: messages
  datasource:
    url: jdbc:h2:mem:testdb

jpa:
  show-sql: true
  hibernate:
    ddl-auto: create
  generate-ddl: true

h2:
  console:
    enabled: true